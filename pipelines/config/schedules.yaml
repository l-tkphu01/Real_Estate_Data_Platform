# ─────────────────────────────────────────────────────────────────
# ROLE: Lập lịch (Scheduling)
# FILE: schedules.yaml
# DESCRIPTION: Cấu hình Blueprint thời gian (cron) cho các luồng xử lý tự động.
# ─────────────────────────────────────────────────────────────────

schedules:
  ingestion_hourly:
    cron: "0 * * * *"
  processing_daily:
    cron: "30 1 * * *"
