# Pipeline Config

Thư mục này chứa các cấu hình cho orchestration, schedule và run settings.

Gợi ý file nên có khi bắt đầu coding:
- `run_config_local.yaml`: profile cấu hình chạy local.
- `schedules.yaml`: cấu hình lịch chạy định kỳ.
- `alerts.yaml`: cấu hình cảnh báo và SLA.
